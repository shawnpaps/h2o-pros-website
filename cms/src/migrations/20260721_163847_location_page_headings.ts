import { MigrateUpArgs, MigrateDownArgs, sql } from '@payloadcms/db-vercel-postgres'

export async function up({ db, payload, req }: MigrateUpArgs): Promise<void> {
  await db.execute(sql`
   ALTER TABLE "site_settings" ADD COLUMN "city_page_headline" varchar;
  ALTER TABLE "site_settings" ADD COLUMN "county_page_headline" varchar;`)
}

export async function down({ db, payload, req }: MigrateDownArgs): Promise<void> {
  await db.execute(sql`
   ALTER TABLE "site_settings" DROP COLUMN "city_page_headline";
  ALTER TABLE "site_settings" DROP COLUMN "county_page_headline";`)
}
